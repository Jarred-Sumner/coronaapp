{
  "name": "corona-time",
  "license": "MIT",
  "engines": {
    "node": "13.10",
    "yarn": "1.19"
  },
  "scripts": {
    "build": "yarn --cwd frontend install && yarn --cwd frontend build-web",
    "deploy": "cp -a frontend/out/. public/",
    "heroku-postbuild": "yarn build && yarn deploy"
  },
  "dependencies": {
    "next-images": "^1.3.1",
    "react-helmet": "^5.2.1"
  }
}
