{
  "name": "corona-time",
  "license": "MIT",
  "engines": {
    "node": "10.15.3",
    "yarn": "1.15.2"
  },
  "scripts": {
    "build": "yarn --cwd frontend install && yarn --cwd frontend build-web",
    "deploy": "cp -a frontend/web-dist/. public/",
    "heroku-postbuild": "yarn build && yarn deploy"
  }
}
